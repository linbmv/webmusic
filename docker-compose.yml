services:
  webmusic:
    build: .
    image: webmusic:latest
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      HOST: "0.0.0.0"
      # Optional: required only when using the Karpov provider.
      # KARPOV_API_KEY: ""
    restart: unless-stopped
